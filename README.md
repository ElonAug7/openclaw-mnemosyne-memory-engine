# Project-Mnemosyne-for-openclaw-
Zero-dependency local memory engine for OpenClaw. 271KB runtime, no LLM API key, no vector DB. TF-IDF + regex hybrid scoring, 7-way parallel search, built-in Web UI. Pure Markdown storage, git-friendly. Linux/macOS（I havnt try macOS）. MIT.
